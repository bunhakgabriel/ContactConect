<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../css/criarContato.css"> 
    <%# Não é necessario colocar a pasta 'public' no caminho do css pois o express automaticamente vai procurar um arquico css dentro de uma pasta public' %>
    <title>Document</title>
</head>
<body>
    
    <header>
        <ul>
            <li><a href="/">Contatos</a></li>
            <li><a href="/logout">Sair</a></li>
        </ul>
    </header>

    <div class="container">

        <h1>Crie o seu contato abaixo</h1>

        <% if(error.length > 0) { %>
            <% error.forEach( err => { %>
                <h3 class="error"><%= err %></h3>
        <% }) %>
        <% } else if(success.length > 0){ %>
            <h3 class="success"><%= success %></h3>
        <% } %>

        <form action="/criarContato" method="post">
            <input type="text" name="nome" placeholder="Nome">
            <input type="text" name="sobrenome" placeholder="Sobrenome">
            <input type="text" name="email" placeholder="Email">
            <input type="tel" name="telefone" placeholder="Telefone">
            <button type="submit">Criar Contato</button>
        </form>

    </div>

</body>
</html>